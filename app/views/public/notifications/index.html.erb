<h1>通知一覧</h1>
<% if current_user.notifications.where(read: false).any? %>
  <% @notifications.each do |notification| %>
    <%= link_to notification_message(notification), notification_path(notification), data: { turbo_method: :patch } %><br>
  <% end %>
<% else %>
  <p>未読の通知はありません</p>
<% end %>
